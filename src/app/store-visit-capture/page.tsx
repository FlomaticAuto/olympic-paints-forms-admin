import StoreVisitCaptureForm from '@/components/StoreVisitCaptureForm';

export const metadata = {
  title: 'Store Visit Capture — Olympic Paints',
};

export default function StoreVisitCapturePage() {
  return <StoreVisitCaptureForm />;
}
